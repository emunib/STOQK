<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <span class="navbar-brand mb-0 h1">STOQK</span>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav">
            <li class="nav-item {{#ifEq name "english"}}active{{/ifEq}}">
                <a class="nav-link" href="/english">English<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item {{#ifEq name "urdu"}}active{{/ifEq}}">
                <a class="nav-link" href="/urdu">Urdu</a>
            </li>
            <li class="nav-item {{#ifEq name "vocabulary"}}active{{/ifEq}}">
                <a class="nav-link" href="/vocabulary">Vocabulary</a>
            </li>
        </ul>

        <form class="form-inline my-0 mr-auto ml-auto">
            <select id="chapter-select" class="selectpicker" title="Choose a chapter" data-size="10">
                {{#each options}}
                    <option value="{{val}}">{{text}}</option>
                {{/each}}
            </select>

            <label for="from-verse-select">From</label>
            <select id="from-verse-select" class="selectpicker" title="Choose a verse" data-size="10">
                {{#each options}}
                    <option value="{{val}}">{{text}}</option>
                {{/each}}
            </select>

            <label for="to-verse-select">To</label>
            <select id="to-verse-select" class="selectpicker" title="Choose a verse" data-size="10">
                {{#each options}}
                    <option value="{{val}}">{{text}}</option>
                {{/each}}
            </select>

            <button class="btn btn-primary my-2 my-sm-0" type="submit">Submit</button>
        </form>

        <div>
            <button class="btn btn-primary">Sub</button>
            <button class="btn btn-primary">Sub</button>
            <button class="btn btn-primary">Sub</button>
        </div>
    </div>
</nav>